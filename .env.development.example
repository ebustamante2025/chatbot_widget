# =============================================================================
# Solo Vite en localhost: copiar a .env.development → npm run dev
# (Docker / producción usan .env — ver .env.example)
# =============================================================================

VITE_API_URL=http://localhost:3004/api

# Opcional en local (si no usa los del .env de Docker)
# VITE_ISA_REGISTRO_WEBHOOK_URL=
# VITE_ISA_AGENT_WEBHOOK_URL=
# VITE_FAQ_URL=http://localhost:3008
